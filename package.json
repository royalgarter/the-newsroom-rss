{
  "scripts": {
    "start": "deno run --unstable-kv --allow-net --allow-read --allow-env --env-file=.env backend/server.ts",
    "local": "deno task tailwind;deno task start;",
    "tailwind": "npx @tailwindcss/cli -m -i ./frontend/style.css -o ./frontend/index.css --content \"frontend/**/*.html,./frontend/**/*.js\";",
    "tailwind:install": "npm install --no-save tailwindcss@4 @tailwindcss/cli",
    "update-preset": "deno run --allow-read --allow-write backend/src/manage-presets.ts update",
    "check-preset": "deno run --allow-net --allow-read --allow-write backend/src/manage-presets.ts check",
    "check-preset-full": "cp data/preset.json data/preset-full.json && deno run --allow-net --allow-read --allow-write backend/src/manage-presets.ts check"
  }
}
